It runs with not the latest version of execution files you gave.

The marker can reach the middle line, whereas red marker cannot place units there,
because units can only be in its own field.

press control+c to end game_admin

#TO RUN:
clear
./flush
make
./run.sh